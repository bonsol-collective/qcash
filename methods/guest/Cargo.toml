[package]
name = "guest"
version = "0.1.0"
edition = "2024"

[dependencies]
risc0-zkvm = { version = "3.0.4", default-features = false, features = ["std"] }
qcash_core = { path = "../../core" }
sha2 = { version = "0.10", default-features = false }
